# ── Python ──────────────────────────────────────────────────────────────────
__pycache__/

# ── Virtual environments ─────────────────────────────────────────────────────
.venv/
.vl_venv/
.lang_venv/
.tts/
backend/venv/

# ── CORTEX runtime data ──────────────────────────────────────────────────────
chroma_db/
rl_feedback/
downloaded_images/
logs/
youtube/
wandb/

# ── ML assets (large binaries) ───────────────────────────────────────────────
models/
model/
NeMo/

# ── Source data (documents & training sets) ──────────────────────────────────
data/
dataset/
# Allow the classifier training set and trained model — needed for CI retraining
!tf-idf_classifier/dataset/
!tf-idf_classifier/model/

# ── Node / web_ui ────────────────────────────────────────────────────────────
node_modules/

# ── Backend ──────────────────────────────────────────────────────────────────
backend/pg_data/
backend/__pycache__/

# ── File types ───────────────────────────────────────────────────────────────
*.odt
*.pdf
*.log
*.txt
*.wav
*.json
*.jsonl
# *.joblib: ignore serialised sklearn models everywhere EXCEPT the classifier
*.joblib
!tf-idf_classifier/model/*.joblib

# ── Secrets & local config ───────────────────────────────────────────────────
.env
.python-version

# ── Notebooks ────────────────────────────────────────────────────────────────
testing.ipynb

# ── Exceptions ───────────────────────────────────────────────────────────────
!requirements.txt
